const newActivity = require("./search");
$(document).ready(() => {
    if (newActivity !== "") {
        console.log("booyah")
    } else {
        console.log("suckah")
    }
  $(document).ready(() => {
    });